name: new-app-harness-example
replicas: 1

secretName: webapp-secrets

createNamespace: true
namespace: ${infra.kubernetes.namespace}

env:
  secrets:
    username: 'test'
    password1: 'test'
  config:
    config_properties: ${configFile.getAsString("config_properties")}
