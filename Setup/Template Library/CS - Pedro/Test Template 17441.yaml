harnessApiVersion: '1.0'
type: SHELL_SCRIPT
scriptString: |-
  echo "########## EXECUTING GIT OPERATIONS IN ${GITHUB_REPO} #############"
  echo ${GITHUB_SSH_TOKE}
  echo "########## FINISHED ##############"
scriptType: BASH
secretOutputVars: SECRETS_VAR
timeoutMillis: 600000
variables:
- name: GITHUB_SSH_TOKEN
  value: ${secrets.getValues("gitPasswordDefault")}
- name: GITHUB_REPO
  value: defaul-repo
